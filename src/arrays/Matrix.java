package arrays;

import java.util.Arrays;
import java.util.Random;

public class Matrix {

	public static void main(String[] args) {

		//Q5Arrays
		//		Random rand = new Random();
//		int[][] matrix = new int[20][10];
//		int sum = 0;
//		for (int i = 0; i < matrix.length; i++) {
//			int studentSum = 0;
//			for (int j = 0; j < matrix[i].length; j++) {
//				matrix[i][j] = rand.nextInt(46) + 55;
//				studentSum += matrix[i][j];
//			}
//			int avg = studentSum / matrix[i].length;
//			System.out.println("The grades " + Arrays.toString(matrix[i]));
//			System.out.println("The student #" + i + " average is " + avg);
//			sum += avg;
//		}
//		int schoolAvg = sum / matrix.length;
//		System.out.println("The school average is " + schoolAvg);

		// BAsicMatrix
		// Random rand = new Random();
		// int[][] matrix = new int[2][3];
		// for (int i = 0; i < matrix.length; i++) {
		// for (int j = 0; j < matrix[i].length; j++) {
		// matrix[i][j] = rand.nextInt(9)+1;
		// }
		// }
		// for (int i = 0; i < matrix.length; i++) {
		// System.out.println(Arrays.toString(matrix[i]));
		// }
	}

}
